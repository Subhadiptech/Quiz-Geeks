<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ersubhadip.quizgeeks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizGeeks">
        <activity android:name=".Teacher.TeacherAddDataActivity"></activity>
        <activity android:name=".Teacher.MainTeacherActivity" />
        <activity android:name=".Teacher.SignUpteacher" />
        <activity android:name=".Teacher.TeacherAuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student.StudentsAuthActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>